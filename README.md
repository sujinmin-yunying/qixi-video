# 如果我们的故事上映了

静态页面可以直接预览活动流程；真实电影海报生成需要通过带有 `/api` 服务的地址访问，不能使用 `file://` 直接调用。

## 配置图像生成

1. 将项目部署到 Vercel。
2. 在项目环境变量中配置 `OPENAI_API_KEY`。
3. 重新部署后，页面会通过服务端调用 `gpt-image-2` 生成完整电影画面。

密钥只存在服务端，禁止写入 `app.js` 或提交到 GitHub。
